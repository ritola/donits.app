---
---
# Access Git repository

Let the user select a remote git repository and list the files in `doc/done` and `doc/backlog` folders from `main` and `backlog` branches.

Use `isomorphic-git` npm library.
